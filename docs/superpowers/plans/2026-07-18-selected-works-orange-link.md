# Selected Works Orange Link Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Display every “Click to know more” label in Selected Works using the portfolio's orange theme color.

**Architecture:** Keep the existing shared `.work-more` class and change only its default color declaration. The existing hover selector and all card structure and behavior remain unchanged.

**Tech Stack:** HTML generated by vanilla JavaScript, CSS custom properties, Node.js built-in test runner

## Global Constraints

- Use the existing `--orange` theme token in both light and dark themes.
- Do not change Selected Works layout, typography, copy, or interaction behavior.
- Preserve all unrelated uncommitted workspace changes.

---

### Task 1: Set the Selected Works detail label to orange

**Files:**
- Modify: `styles.css:275`
- Test: `tests/selected-works.test.js`

**Interfaces:**
- Consumes: the existing `.work-more` class and `--orange` CSS custom property
- Produces: orange default text color for all three Selected Works detail labels

- [ ] **Step 1: Write the failing test**

```js
const test = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');

const css = fs.readFileSync(path.resolve(__dirname, '..', 'styles.css'), 'utf8');

test('Selected Works know-more labels use the orange theme color', () => {
  assert.match(css, /^\.work-more\s*\{[^}]*color:var\(--orange\)/m);
});
```

- [ ] **Step 2: Run the focused test and verify it fails**

Run: `node --test tests/selected-works.test.js`

Expected: FAIL because `.work-more` currently uses `var(--muted)`.

- [ ] **Step 3: Implement the minimal CSS change**

Change the default declaration to:

```css
.work-more{font:500 11px var(--mono);letter-spacing:.06em;color:var(--orange);transition:color .2s,letter-spacing .2s}
```

- [ ] **Step 4: Verify the focused test and full suite**

Run: `node --test tests/selected-works.test.js && node --test tests/*.test.js`

Expected: both commands exit 0 with no failing tests.

- [ ] **Step 5: Review the scoped diff**

Run: `git diff -- styles.css tests/selected-works.test.js`

Expected: only the `.work-more` default color and its regression test are added for this task.
